---

apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: github
spec:
  service:
    ports:
    - port: 12000
      targetPort: 12000
  github:
    argo-combined-app:
      owner: siminn-johanngj
      repository: argo-combined-app
      webhook:
        endpoint: /argo-combined-app
        port: "12000"
        method: POST
        url: http://webhook.af52ff8dc45d242e8b4090f0b8682ee8-acaa29feaf0deaa5.elb.eu-west-1.amazonaws.com
      events:
        - "*"
      apiToken:
        name: github-access
        key: token
      insecure: true
      active: true
      contentType: json

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: github
  annotations:
    kubernetes.io/ingress.class: "nginx"
    ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
           name: github-eventsource-svc
           port:
             number: 12000
    host: webhook.af52ff8dc45d242e8b4090f0b8682ee8-acaa29feaf0deaa5.elb.eu-west-1.amazonaws.com
